# Gitleaks config. Inherits default rules and adds project-specific allowlist.
# Docs: https://github.com/gitleaks/gitleaks#configuration

[extend]
useDefault = true

[allowlist]
description = "Project-specific allowlist"
paths = [
    '''\.env\.example$''',
    '''tests/.*fixtures.*''',
]
